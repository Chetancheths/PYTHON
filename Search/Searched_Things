# how to compare -two-dictionaries-in-python and if key are same -set the value as 1


shared_items = {k: x[k] for k in x if k in y and x[k] == y[k]}

dict - list

for key in conf.keys():
    if key in codebranch_keys:
         conf[key] = 1
